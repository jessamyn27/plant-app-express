<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title></title>
  <link rel="stylesheet" type="text/css" href="/css/style.css" />
  <link href="https://fonts.googleapis.com/css?family=Ubuntu" rel="stylesheet">
</head>

<body>
  <h1>Plants Index Page</h1>
  <ul>
    <% for(let i = 0; i < plants.length; i++){ %>
      <li>
        The
        <a href="/plants/<%=plants[i].id%>">
          <%=plants[i].name; %>
        </a> is
        <%=plants[i].color; %>.
          <% if(plants[i].sunnySpot === true){ %>
            show it some sunshine
            <% } else { %>
              give it some shade
              <% } %>
                <form method="POST" action="/plants/<%=plants[i].id%>?_method=DELETE">
                  <input type="submit" value="DELETE">
                </form>
                <a href="/plants/<%=plants[i].id%>/edit">Edit Plant</a>
      </li>
      <% } %>
  </ul>
  <nav>
    <a href="/plants/new">Create a New Plant</a>
  </nav>
</body>

</html>
